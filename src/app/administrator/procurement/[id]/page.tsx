import React from 'react'

function Page(props: { params: { id: string }, searchParams: { [key: string]: string | string[] | undefined } }) {

    return (
        <>
        </>
    )
}

export default Page
